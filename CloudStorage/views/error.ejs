
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Cloud Storage App</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-danger text-white">
                        <h3 class="mb-0">Error</h3>
                    </div>
                    <div class="card-body">
                        <h4 class="card-title mb-4">Something went wrong</h4>
                        <div class="alert alert-danger">
                            <p><strong>Error:</strong> <%= error.message %></p>
                            <% if (process.env.NODE_ENV !== 'production') { %>
                                <pre class="mt-3 bg-light p-3 rounded"><%= error.stack %></pre>
                            <% } %>
                        </div>
                        <div class="mt-4">
                            <a href="/dashboard" class="btn btn-primary">Back to Dashboard</a>
                            <a href="/" class="btn btn-outline-secondary ms-2">Go to Login</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
